{% extends 'base.html.twig' %}

{% block title %}commande reussie {% endblock %}

{% block body  %}
    <div class="header-margin"></div>
    <div class="container">
        <div class="d-flex align-items-center justify-content-center pb-2">
            <img src="/image/ok.png" alt="" style="width: 30vh">
        </div>

    <div class="alert alert-success mt-5" role="alert" >
        <h4 class="alert-heading ">Bravo!</h4>
        <p>merci  {{ order.user.lastname}} {{ order.user.firstname}} pour votre commande n°{{ order.reference}} </p>
        <hr>
        <p class="mb-0">votre commande sera traité par nos équipe dans le meilleur des délais et un email de confirmation vous sera envoyer sur votre adresse  mail
            {{ order.user.email }}
        </p>
    </div>
        <div class="d-flex justify-content-center">
            <a href="{{ path('products') }}" class="btn btn-success mr-2">continuer mes achats</a>
            <a href="{{ path('home') }}" class="btn btn-primary ">accueil </a>
        </div>
    </div>
{% endblock %}
